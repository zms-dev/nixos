# dev

Development tooling — language servers, runtimes, and developer-facing utilities.

Each aspect provides something a developer needs in their environment: a language server for editor integration, a runtime for a specific language, or a CLI tool that targets the Nix ecosystem itself. These are distinct from general CLI tools in that they serve a programming workflow rather than general system use.
