# groups

**User Groups — Declarative configurations for system user groups.**

This directory defines system-wide user groups and their associated IDs (GIDs). These configurations ensure consistent group IDs and permissions across different hosts and environments, distinct from individual user settings which are located under the users/ directory.
