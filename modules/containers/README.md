# containers

**Containers — Declarative systemd-nspawn container configurations.**

This directory contains configurations for isolated containerized environments and services, primarily managing self-hosted server and home-lab applications. Unlike system-wide services, these modules structure programs within systemd-nspawn containers to isolate network interfaces, permissions, and environments from the host system.
