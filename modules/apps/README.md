# apps

GUI applications — programs with graphical interfaces that users interact with directly.

Each aspect installs and configures one application: its package, home-manager program settings, extensions or plugins, and any system-level requirements (D-Bus permissions, portal config, etc.). Applications here are distinct from CLI tools in that they have a graphical interface, and from GUI infrastructure (compositor, bar, notifications) which lives under `gui/`.
