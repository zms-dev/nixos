# fonts

Font packages — typefaces installed system-wide via home-manager.

Each aspect installs one font family and registers it for use across the system. Fonts here cover a range of purposes: UI text, monospace/coding, icon glyphs (for use in bars and terminals), and emoji. Stylix picks up whatever is installed here for its font configuration.
