# cli

Command-line tools and shell configuration.

Each aspect installs and configures one terminal-based program: its package, shell aliases, environment variables, and config files managed via home-manager. Also includes the shell itself and its prompt. Neovim lives here as a sub-namespace with its configuration split across focused sub-modules (options, keymaps, LSP, plugins, etc.), assembled by the top-level neovim aspect.
