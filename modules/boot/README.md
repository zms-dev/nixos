# boot

**Boot & Storage — Disk partitioning and ephemeral storage configuration.**

This directory configures how the system boots, partitions disks, and handles storage persistence. It includes configurations for disko to manage declarative partitioning and impermanence for ephemeral system states, separating underlying disk configuration and early boot setup from higher-level applications and system services.
