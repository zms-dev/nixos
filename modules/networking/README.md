# networking

**Networking — System network interfaces, routing, and subnets configuration.**

This directory manages networking configuration, including physical and virtual network interfaces, bridges, bonds, NAT routing, and NetworkManager settings. It provides the core networking topology and infrastructure necessary to connect the hosts, separating base network routing and addressing from service-level connection protocols.
