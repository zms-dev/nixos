# style

System-wide theming — color scheme and font application across all configured programs.

Each aspect contributes one piece of the theming pipeline: a collection of Base16 color scheme definitions to choose from, and the Stylix layer that applies the chosen scheme and fonts to every program that supports it (terminal, editor, browser, GTK apps, etc.). Changing the active scheme here propagates to the whole system on rebuild.
